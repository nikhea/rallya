// Package kit implements event kit (merch/welcome-pack) collections.
// Organizers define named kit types per event; door staff hand units to
// checked-in attendees through PENDING -> COLLECTED (-> VOIDED) records.
package kit
